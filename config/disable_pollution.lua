--- Lists all bonuses which can be used, name followed by min max
-- @config Disable Pollution

return {
    no_biters=true, -- True if requiring the map to be clear of biters first
    max_enemies=50 -- The maximum number of non-spawner enemies that may be present on the map for the command to still succeed
}