--- Config file for the waterfill command
-- @config Waterfill

return {
    max_range=50, --- @setting max_range the max range that can be used with this command
    require_spectate=true --- @setting require_spectate Whether the user must be in /spectate to use this command
}
