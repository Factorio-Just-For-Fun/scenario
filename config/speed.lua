--- Config file for the speed command
-- @config Speed

return {
    max_speed=1, --- @setting max_speed the maximum game speed that can be used with the speed command
    min_speed=0.4 --- @setting min_speed the minimum game speed that can be used with the speed command
}
